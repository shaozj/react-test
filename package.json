{
  "name": "react-test",
  "private": true,
  "version": "0.0.1",
  "description": "react-test - Generated by generator-react-multipage",
  "main": "src/index.js",
  "scripts": {
    "start": "silk server",
    "test": "silk test",
    "posttest": "npm run lint",
    "build": "silk build",
    "lint": "eslint ./src",
    "clean": "rimraf build/*",
    "release:major": "npm version major && npm publish && git push --follow-tags",
    "release:minor": "npm version minor && npm publish && git push --follow-tags",
    "release:patch": "npm version patch && npm publish && git push --follow-tags",
    "fastpush": "npm run build && git add . && git commit -m 'build' && git push origin daily/1.0.0"
  },
  "repository": "",
  "keywords": [],
  "author": "Your name here",
  "devDependencies": {
    "babel-plugin-import": "^1.1.0"
  },
  "dependencies": {
    "antd": "^2.8.1",
    "classnames": "^2.2.5",
    "core-js": "^2.0.0",
    "normalize.css": "^4.0.0",
    "react": "^15.0.0",
    "react-dom": "^15.0.0",
    "whatwg-fetch": "^2.0.1",
    "fetch-jsonp": "^1.0.5"
  }
}
